{
  "name": "monorepo-template",
  "private": true,
  "packageManager": "npm@10.8.2",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "start": "turbo run start"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "turbo": "^2.5.0"
  }
}
