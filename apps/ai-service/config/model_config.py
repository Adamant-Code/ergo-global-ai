

EMBEDDING_DIMENSIONS = {
    "text-embedding-3-large": 1536,  # 1536 dimensions for the large model.
    "text-embedding-3-small": 768,   # 768 dimensions for the small model.
}

COST_PER_TOKEN = {
    "text-embedding-3-large": 0.0001,  # Cost per token for the large model.
    "text-embedding-3-small": 0.00005,  # Cost per token for the small model.
}
